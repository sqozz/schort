[project]
name = "schort"
version = "0.1.0"
description = "A tiny link shortener written in python and flask"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "requests>=2.34.0",
    "ruff>=0.14.7",
]
